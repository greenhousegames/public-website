# Gulp will reference these paths when it copies files
PATHS:
  # Path to dist folder
  dist: "public"
  # Paths to game repos
  games:
    smashdot:
      npm: 'smash-dot-cordova'
      dist: 'smashdot'
